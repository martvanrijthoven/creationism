## needs to be in creationism, with argument e.g., WholeSlideDataConfiguration
def validate(user_config):
    ...
    # check Configuration key

        # check mode keys

            # check if for every key key are in default config keys (split @replace)

                # if @replace, 
                    # check if module , attribute or registrant ..

                # if module/attribute check if exists

                # if registrant.. check if exists

                # check value 
                    # check if @{}check